{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug sst_cam",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/debug/bin/sst_cam",
      "args": [],
      "cwd": "${workspaceFolder}"
    }
  ]
}