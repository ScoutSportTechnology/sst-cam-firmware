{
  "version": "2.0.0",
  "inputs": [
    {
      "id": "preset",
      "type": "pickString",
      "description": "CMake preset",
      "options": [
        "debug-x64",
        "test-x64",
        "release-x64",
        "debug-x86",
        "test-x86",
        "release-x86",
        "debug-arm64",
        "test-arm64",
        "release-arm64",
        "debug-arm",
        "test-arm",
        "release-arm"
      ]
    },
    {
      "id": "host_arch",
      "type": "pickString",
      "description": "Host tools arch",
      "options": [
        "amd64",
        "x86",
        "arm64",
        "arm"
      ]
    }
  ],
  "windows": {
    "options": {
      "shell": {
        "executable": "cmd.exe",
        "args": [
          "/C",
          "${workspaceFolder}\\scripts\\task_parse.cmd ${input:preset}",
          "&&",
          "echo target_arch=%target_arch%",
          "&&",
          "\"C:/Program Files/Microsoft Visual Studio/18/Community/Common7/Tools/VsDevCmd.bat\"",
          "-arch=%target_arch%",
          "-host_arch=${input:host_arch}",
          "&&"
        ]
      }
    }
  },
  "tasks": [
    {
      "label": "configure",
      "type": "shell",
      "command": "cmake",
      "args": [
        "--preset",
        "${input:preset}"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      }
    },
    {
      "label": "build",
      "type": "shell",
      "command": "cmake",
      "args": [
        "--build",
        "--preset",
        "${input:preset}"
      ],
      "dependsOn": [
        "configure"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      }
    },
    {
      "label": "clean",
      "type": "shell",
      "command": "cmake",
      "args": [
        "--build",
        "--preset",
        "${input:preset}",
        "--target",
        "clean"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      }
    }
  ]
}