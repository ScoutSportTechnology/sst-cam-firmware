mod common;
//use common::logger::Logger;
fn main() {/*
    let log = Logger::new("sst-cam");
    log.info("Logger info message", &[]);
    log.error("Logger error message", &[]);
    log.warning("Logger warning message", &[]);
    log.debug("Logger debug message", &[]);*/
}
